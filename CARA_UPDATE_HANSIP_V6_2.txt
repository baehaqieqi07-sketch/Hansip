# CARA UPDATE HANSIP V6.2

Yang diubah:
- Hapus command hbj
- Hapus command hcf
- Hapus command hnpc
- Hapus command hteam
- Hapus command hdaily
- Hapus command hshop
- Semua warna embed jadi #7DBD77

Cara pasang:

```bat
cd /d "D:\"
rmdir /s /q "Hansip"
```

Extract ZIP ini ke D:\ sampai jadi D:\Hansip.

```bat
cd /d "D:\Hansip"
node --check index.js
npm install
git init
git branch -M main
git remote add origin https://github.com/baehaqieqi07-sketch/Hansip.git
git add .
git commit -m "remove selected Hansip commands and set embed color"
git push -f origin main
```

Kalau remote sudah ada:

```bat
git remote set-url origin https://github.com/baehaqieqi07-sketch/Hansip.git
git push -f origin main
```
